<div class="w-25 h-100"></div>
<div class="d-flex flex-column position-fixed w-25 h-100 overflow-y-auto _navigation">
    <a class="_link" href="{{ path('app_home') }}">Вернуться к поиску</a>
    {% if 'ROLE_USER' in user.roles %}
        <hr class="my-3">
        <a class="_link" href="{{ path('auth_account') }}">Функций для пользователя в данный момент не существует</a>
    {% endif %}
    {% if 'ROLE_ADMIN' in user.roles %}
        <hr class="my-3">
        <a class="_link" href="{{ path('admin_loaded_serials') }}">Отобразить загруженные серии</a>
    {% endif %}
</div>
